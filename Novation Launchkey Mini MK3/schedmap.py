"""import variables as var, pads
from general import undoUp
from time import time, sleep

def Trigger():
    for x,y in var.repeatok.items():
        if y:
            print(var.repeatok)
            print (var.repeatqueue.values())
            print (var.currenttime)
            if x == "sespad15_DATA1":
                    undoUp()
"""